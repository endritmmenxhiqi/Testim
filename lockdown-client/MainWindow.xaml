<Window x:Class="LockdownBrowser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="BGT Secure Exam Browser" Height="1080" Width="1920"
        WindowStyle="None" 
        WindowState="Maximized" 
        ResizeMode="NoResize" 
        Topmost="True" 
        ShowInTaskbar="False"
        Loaded="Window_Loaded" Closing="Window_Closing">

    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/> <!-- Top Bar -->
            <RowDefinition Height="*"/>   <!-- Browser -->
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <Border Grid.Row="0" Background="#2196F3" Padding="20,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Student Info -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="40" Height="40" CornerRadius="20" Background="#4CAF50" Margin="0,0,12,0">
                        <TextBlock x:Name="AvatarLetter" Text="S" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="StudentNameTxt" Text="Student Name" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                        <TextBlock Text="BGT Secure Environment" Foreground="#8888AA" FontSize="12"/>
                    </StackPanel>
                </StackPanel>

                <!-- Timer -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <materialDesign:PackIcon Kind="Clock" Foreground="White" Width="30" Height="30" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TimerTxt" Text="01:30:00" Foreground="White" FontSize="20" FontWeight="Bold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Finish Button -->
                <Button Grid.Column="3" Content="Përfundo Provimin" Click="FinishExam_Click"
                        Background="#D32F2F" Foreground="White" BorderBrush="Transparent"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        ToolTip="Submit and Exit Exam"/>
            </Grid>
        </Border>

        <!-- WebView2 Browser -->
        <wv2:WebView2 Grid.Row="1" Name="ExamBrowser" Source="about:blank"/>
    </Grid>
</Window>
